# Jiayi Zhang Homepage

Static homepage (no Jekyll/Ruby dependency).

## Local Preview

```bash
npm run dev
```

Open `http://127.0.0.1:4000`.

## Deploy

Push to `main` branch of `didiforgithub.github.io`.
GitHub Pages will serve the root static files directly.
